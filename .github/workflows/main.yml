name: Authentication Service

on:
  push:
    branches:
      - dev  

jobs:
  build:
    runs-on: ubuntu-latest

    steps:
    - name: Checkout code
      uses: actions/checkout@v2

    - name: Install dependencies
      run: npm install

    - name: Deploy to server
      run: |
        ssh gaiyadev@143.198.0.200 'cd do-cicd && git pull origin main && npm install && npm start'
      env:
        GH_TOKEN: ${{ secrets.DIGITALOCEAN_TOKEN }}
